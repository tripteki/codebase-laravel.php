<div class="panel panel-note note-box mb-4">
    <p class="note-box__title fw-semibold small mb-2">{{ $title ?? __('auth.verify_email_security_tip') }}</p>
    <p class="small text-muted mb-0">{{ $body }}</p>
</div>
